<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/styles.css">
  <title>New Message</title>
</head>

<body>
  <main class="form">
    <section class="new-message">
      <form class="form-container" action="/new-message" method="POST">
        <p class="title">Create New Message</p>
        <div class="input-group">
          <label for="title">Title</label>
          <input type="text" name="title" id="title" value="<%= typeof formData !== 'undefined' ? formData.title:'' %>" required />
        </div>
        <div class="input-group">
          <label for="text">Message</label>
          <textarea name="text" id="text" cols="26" rows="7" value="<%= typeof formData !== 'undefined' ? formData.text:'' %>" required></textarea>
        </div>
        <% if(typeof errors!=='undefined' && errors.length > 0) { %>
        <div class="errors">
          <% errors.forEach(function(error) {%>
          <p class="error"><%=error.msg%></p>
          <%}) %>
        </div>
        <% } %>
        <button class="sign" type="submit">Create Message</button>
        <a class="back-btn" href="/">Cancel</a>
      </form>
    </section>
  </main>
</body>

</html>